"Spaces and tabs
set tabstop=2 "number of spaces per tab
set shiftwidth=2 "number of spaces to apply when using the reindent operations
set softtabstop=2 "number of spaces per tab when editing

"Miscellaneous
filetype indent off
